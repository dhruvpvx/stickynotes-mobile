import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

type Props = {};

const AddTextNote = (props: Props) => {
  return (
    <View>
      <Text>AddTextNote</Text>
    </View>
  );
};

export default AddTextNote;

const styles = StyleSheet.create({
  container: {},
});
