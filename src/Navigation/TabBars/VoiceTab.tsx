import { StyleSheet, Text, View } from 'react-native'
import React from 'react'
import { Container } from '../../Copmponets'

type Props = {}

const VoiceTab = (props: Props) => {
  return (
    <Container>
      <Text>VoiceTab</Text>
    </Container>
  )
}

export default VoiceTab

const styles = StyleSheet.create({})